# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "4.0"
metadata:
  name: nwproducts
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          destination: Daksha
          url: http://s4h2023.sapdemo.com:8023
          client: '100'
        app:
          name: ZFIORIPRACT1
          description: fiori practise
          package: zsumanv
          transport: A4HK904788
        exclude:
          - /test/
